---
- name: test k8s-worker deployment
  hosts:
    - worker0
    - worker1
    - worker2

  roles:
    - role: cfssl
      vars:
        cfssl_certs_path: /var/lib/kubernetes/certs

    - role: k8s-worker
      vars:
        kubelet_certs_path: /var/lib/kubernetes/certs